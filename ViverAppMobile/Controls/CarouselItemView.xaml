<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="ViverAppMobile.Controls.CarouselItemView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">
    <Border Stroke="Gold" StrokeShape="RoundRectangle 8">
        <Grid HorizontalOptions="Fill" VerticalOptions="Fill">
            <Image
                Aspect="AspectFill"
                HorizontalOptions="Fill"
                Source="carouselimage1.png"
                VerticalOptions="Fill" />

            <VerticalStackLayout
                Margin="0,23,0,0"
                Padding="15"
                Spacing="40">
                <Grid
                    Margin="5,0,0,0"
                    ColumnDefinitions="Auto,*"
                    ColumnSpacing="20">
                    <Border
                        Grid.Column="0"
                        Padding="10,10,10,8"
                        BackgroundColor="Gold"
                        Stroke="Transparent"
                        StrokeShape="RoundRectangle 14"
                        VerticalOptions="Center">
                        <Label
                            FontFamily="AppIcons"
                            FontSize="28"
                            Text="s"
                            TextColor="White" />
                    </Border>

                    <VerticalStackLayout
                        Grid.Column="1"
                        Spacing="4"
                        VerticalOptions="Center">

                        <VerticalStackLayout.Margin>
                            <OnPlatform>
                                <On Platform="Android" Value="6,0,0,0" />
                                <On Platform="WinUI" Value="0" />
                            </OnPlatform>
                        </VerticalStackLayout.Margin>

                        <Label
                            FontAttributes="Bold"
                            FontSize="18"
                            Text="Meu"
                            TextColor="White" />
                        <Label
                            FontAttributes="Bold"
                            FontSize="12"
                            Text="Penis"
                            TextColor="LightBlue" />
                    </VerticalStackLayout>
                </Grid>

                <Border
                    Margin="12,35,0,0"
                    BackgroundColor="Transparent"
                    Stroke="Transparent"
                    Style="{StaticResource MasterBorderStyle}">
                    <Label
                        FontFamily="Semibold"
                        FontSize="13"
                        LineHeight="1.5"
                        Text="meu pinto é grande e grosso"
                        TextColor="White" />
                </Border>
            </VerticalStackLayout>
        </Grid>
    </Border>
</ContentView>
